# A 阶段建模方案 — <任务标题>

> 由 `aspen-designer` 产出。验收标准：**不得留任何需要建模者临场判断的空白。**
> 所有参数必须是单一确定值，且以「工程单位 | SI 值」双列给出。

- run-id：
- 版本：v1
- 修订记录：（回退修订时在此追加：改了什么 / 为什么）

---

## 1. 模拟目的与精细度

目的类型：`物料平衡估算` / `方案比选` / `设计定型` / `现有装置标定`（选一）

据此确定的模块严格度上限：

---

## 2. 复杂度级别

判定：`L1` / `L2` / `L3`
理由：（模块数、是否有循环、是否有反应、是否电解质/固体）

走的阶段：

---

## 3. 组分体系

| 组分 | Aspen 数据库 ID | 预期实际标签（≤8 字符） | 角色 | 备注 |
|---|---|---|---|---|
|  |  |  | 主反应物 / 产品 / 溶剂 / **惰性** / **杂质** |  |

> 惰性与杂质行必须存在（即使含量 <0.1%），并说明去向：随哪个产品出、是否需 purge。
> 组分名 >8 字符会被 Aspen 截断，modeler 需 `list_components()` 核对实际标签。

---

## 4. 物性方法

- 方法：
- 依据：（引用 `knowledge/engineering-rules.md` §1.1 决策序列第 __ 条）
- 备选方案（若主方案结果可疑时切换）：

### 关键二元对清单（modeler 在 B 阶段必须核查）

| 二元对 | 为什么关键 | 缺失/估算时对结论的影响 |
|---|---|---|
|  |  |  |

---

## 5. 拓扑与分区

```
<文本框图：FEED → H1 → C1 → (DIST / BOTS) ...>
```

| 区 | 功能目标 | 含模块 | 区间界面物流 |
|---|---|---|---|
|  |  |  |  |

### 循环流股（L3 必填）

| 循环流股 | 从 → 到 | 断环阶段假进料值（组成 / T | P | 流量，双列） |
|---|---|---|
|  |  |  |

---

## 6. 模块与参数表

| 模块 | 类型 | 参数 | 工程单位值 | SI 值 | 依据 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

### 进料流股

| 流股 | 参数 | 工程单位值 | SI 值 | 依据 |
|---|---|---|---|---|
| FEED | TEMP |  |  |  |
| FEED | PRES |  |  |  |
| FEED | 总流量 |  |  |  |
| FEED | 组成（逐组分） |  |  |  |

> SI 换算表见 `knowledge/mcp-playbook.md` §1.1。**SI 值由 designer 算，modeler 只读 SI 列。**
> 参数不确定时写"取值 + 依据 + 若失败的备选值"。

---

## 7. 完整压力剖面

| 节点 | 压力（工程单位） | 压力（Pa） | 说明 |
|---|---|---|---|
|  |  |  |  |

> MIXER 不显式设 PRES 会取最低入口压 → 必须给值。这张表是防压力倒挂的唯一办法。

---

## 8. 界区与公用工程

| 项 | 取值 | 来源 |
|---|---|---|
| 加热介质 |  | 用户给定 / **假设**（`engineering-rules.md` §3） |
| 冷却介质 |  |  |
| ΔTmin |  |  |

---

## 9. 验收指标

| # | 指标 | 数值 | 基准 | 允差 | 可达性预判 |
|---|---|---|---|---|---|
| 1 |  |  | 摩尔 / 质量 |  | 无约束 / 共沸限 __ / 平衡限 __ / 温位限 __ |

### 自由度检查
设计变量数： 目标数：
（RADFRAC 只有 2 个规格位。目标数 > 设计变量数 → 方案需增加设计变量）

---

## 10. 建模顺序（编号步骤，modeler 照此执行）

1.
2.
3.

每一步注明：做什么 → 跑什么 → 看什么数判断成功。

---

## 11. 假设清单

| # | 假设内容 | 影响 | 若假设不成立的后果 |
|---|---|---|---|
| 1 |  |  |  |

> 主 agent 会拿这份清单去问用户确认。
